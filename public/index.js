import React from 'react';
import ReactDOM from 'react-dom';
import {Root} from './components/root';

// Simply render the root element.
ReactDOM.render(React.createElement(Root), document.getElementById('root'));
