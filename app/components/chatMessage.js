import React from 'react';
import {Paper, MultilineText, RaisedButton} from 'material-ui';

export default React.createClass({
    render() {
        return (
            <Paper>
                <MultilineText/>
                <RaisedButton/>
            </Paper>
        );
    }
});
